// ==================
// SEQUENCE MENU
// ==================

/**
 * @note We should not use that window, but a specific window for each kind of sequence
 */
window sequence
{
	{
		starlayout	true
		fill		true
	}

	sequence seq
	{
		align		LAYOUTALIGN_FILL
		onClick		{ call *node:root.next }
		onEnd {
			cmd "mn_close sequence"
		}
	}

	confunc seq_start {
		*node:root.seq@src = <1>
		cmd "mn_push sequence"
	}

	confunc seq_end {
		cmd "mn_close sequence"
	}

}
